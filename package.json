{
  "name": "codeguard-ai",
  "version": "1.0.0",
  "description": "CodeGuard AI Monorepo",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\" \"npm run dev --prefix worker\""
  },
  "devDependencies": {
    "concurrently": "^10.0.4"
  }
}
